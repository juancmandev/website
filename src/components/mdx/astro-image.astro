---
import { Image } from "astro:assets";

const { src, alt } = Astro.props;
---

<Image
    src={src}
    alt={alt}
    width={1092}
    height={986}
    class="w-auto h-auto rounded-md aspect-auto"
/>
